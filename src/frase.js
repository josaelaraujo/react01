function frase() {
    return (
        <div>
            <p>Este é o primeiro componente. Aqui eu aprendi que o componente pode usado a qualquer momento desde que eu o export (export default (o nome da função.)) e importe ele na pagina que eu quero usar(import (o nome da
            função.))</p>
        </div>
    )
}

export default frase